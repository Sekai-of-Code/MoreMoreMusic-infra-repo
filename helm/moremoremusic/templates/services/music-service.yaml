{{- if .Values.musicService.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.musicService.name }}
  namespace: {{ .Values.app.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.musicService.name }}
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: {{ .Values.app.name }}
    app.kubernetes.io/version: {{ .Values.musicService.image.tag }}
spec:
  replicas: {{ .Values.musicService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.musicService.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.musicService.name }}
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: {{ .Values.app.name }}
        app.kubernetes.io/version: {{ .Values.musicService.image.tag }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      {{- if .Values.securityContext.enabled }}
      securityContext:
        runAsNonRoot: {{ .Values.securityContext.runAsNonRoot }}
        runAsUser: {{ .Values.securityContext.runAsUser }}
        fsGroup: {{ .Values.securityContext.fsGroup }}
      {{- end }}
      containers:
      - name: {{ .Values.musicService.name }}
        image: "{{ .Values.musicService.image.repository }}:{{ .Values.musicService.image.tag }}"
        imagePullPolicy: {{ .Values.musicService.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.musicService.service.targetPort }}
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        resources:
          {{- toYaml .Values.musicService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        env:
        - name: NODE_ENV
          value: {{ .Values.musicService.env.NODE_ENV | quote }}
        - name: PORT
          value: {{ .Values.musicService.env.PORT | quote }}
        - name: SERVICE_NAME
          value: {{ .Values.musicService.env.SERVICE_NAME | quote }}
        - name: DATABASE_URL
          value: "postgresql://$(DATABASE_USER):$(DATABASE_PASSWORD)@$(DATABASE_HOST):$(DATABASE_PORT)/$(DATABASE_NAME)"
        - name: DATABASE_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: database-host
        - name: DATABASE_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: database-port
        - name: DATABASE_NAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: database-name
        - name: DATABASE_USER
          valueFrom:
            secretKeyRef:
              name: postgresql-secrets
              key: username
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-secrets
              key: password
        - name: S3_BUCKET_NAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: s3-bucket-name
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: aws-region
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws-secrets
              key: access-key-id
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-secrets
              key: secret-access-key
        {{- if .Values.musicService.storage.tempVolume.enabled }}
        volumeMounts:
        - name: temp-storage
          mountPath: /tmp/uploads
        {{- end }}
      {{- if .Values.musicService.storage.tempVolume.enabled }}
      volumes:
      - name: temp-storage
        emptyDir:
          sizeLimit: {{ .Values.musicService.storage.tempVolume.size }}
      {{- end }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.musicService.name }}
  namespace: {{ .Values.app.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.musicService.name }}
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: {{ .Values.app.name }}
spec:
  type: {{ .Values.musicService.service.type }}
  ports:
  - port: {{ .Values.musicService.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  - port: 9090
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app.kubernetes.io/name: {{ .Values.musicService.name }}

{{- if .Values.musicService.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.musicService.name }}-hpa
  namespace: {{ .Values.app.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.musicService.name }}
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: {{ .Values.app.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.musicService.name }}
  minReplicas: {{ .Values.musicService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.musicService.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.musicService.autoscaling.targetCPUUtilizationPercentage }}
  {{- if .Values.musicService.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.musicService.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}

{{- if .Values.podDisruptionBudget.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.musicService.name }}-pdb
  namespace: {{ .Values.app.namespace }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.musicService.name }}
{{- end }}
{{- end }}